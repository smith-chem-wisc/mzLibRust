/target
Cargo.lock

# The staged bridge payload. Built by pyMzLib's publish-bridge.ps1 (or downloaded at build time
# once the release assets exist); never committed — it is ~130 MB of self-contained .NET.
/_dotnet

# Local scratch
*.tmp

# /project workspace scaffolding lives in the PRIVATE workspace repo (trishorts/mzlibrust-project), not here.
.project/
/CLAUDE.md
/RESUME.md

# The replay bridge is a nested package; building it on its own puts a target here.
/tools/replay-bridge/target
