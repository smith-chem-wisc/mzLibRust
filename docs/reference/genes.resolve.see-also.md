<!-- GENERATED by tests/spec_docs.rs from docs/specs/genes.resolve.yaml (bridge 5db922d4cfe1). Do not edit: fix the spec in the bridge, sync it, re-render. -->

# Recorded examples

The examples on this page run in CI against these recordings from the live bridge, replayed by a stand-in bridge that answers only a call the recording fits:

- [`genes_resolve_human.json`](https://github.com/smith-chem-wisc/mzLibRust/blob/main/tests/fixtures/genes_resolve_human.json)

# Same verb in other bindings

| Python (pyMzLib) | Rust (mzLibRust) | R (mzLibR) |
|---|---|---|
| `pymzlib.proteins.resolve_genes` | `mzlib::proteins::resolve_genes_with` with `GeneResolveOptions` | `proteins_resolve_genes` |

# References

- [doi:10.1093/nar/gkaf1239](https://doi.org/10.1093/nar/gkaf1239): Ensembl releases, GTFs and cross-references (Ensembl 2026, NAR)
- [doi:10.1093/nar/gkae1010](https://doi.org/10.1093/nar/gkae1010): UniProt, whose XML dbReferences carry the gene links resolved (UniProt Consortium 2025, NAR)

# Since

| Wire protocol | pyMzLib | mzLibRust | mzLibR |
|---|---|---|---|
| 1 | not yet shipped | next release (the spec has not recorded it yet) | not yet shipped |

# Not yet verified

The spec lists these as open; they are reported, never hidden:

- Rust and R spellings are intended, not yet implemented. The wire module is 'genes' but every binding projects it in its proteins module (BULK.md §6); confirm.
- No verb writes the compact gene set (EnsemblGeneSetWriter); callers need mzLib or a future 'genes gene-set' verb to make one.
- The example gene set is a hand-built three-gene GTF in Ensembl's layout; O43653 is off\_primary\_only only because PSCA is left out of it.
