<!-- GENERATED by tests/spec_docs.rs from docs/specs/readers.read-protein-groups.yaml (bridge 5db922d4cfe1). Do not edit: fix the spec in the bridge, sync it, re-render. -->

# Recorded examples

The examples on this page run in CI against these recordings from the live bridge, replayed by a stand-in bridge that answers only a call the recording fits:

- [`readers_protein_groups.json`](https://github.com/smith-chem-wisc/mzLibRust/blob/main/tests/fixtures/readers_protein_groups.json)

# Same verb in other bindings

| Python (pyMzLib) | Rust (mzLibRust) | R (mzLibR) |
|---|---|---|
| `pymzlib.readers.read_protein_groups`; many inputs: `pymzlib.readers.read_protein_groups_many` | `mzlib::readers::read_protein_groups_with` with `ReadOptions`; many inputs: `mzlib::readers::read_protein_groups_many` | `readers_read_protein_groups`; many inputs: `readers_read_protein_groups_many` |

# References

None: no publication is attached to this verb.

# Since

| Wire protocol | pyMzLib | mzLibRust | mzLibR |
|---|---|---|---|
| 1 | not yet shipped | next release (the spec has not recorded it yet) | not yet shipped |

# Not yet verified

The spec lists these as open; they are reported, never hidden:

- The \_many spellings (py, rust, r) follow the 2026-09-23 cross-binding decision; Rust and R names here are intended, not yet ported, and since.pymzlib stays null until release.
- The group-level columns carried beside each sample (identity, q-value, decoy label) are a choice made here; coverage, masses and counts stay in read-records. Revisit if every binding ends up joining for the same field.
- Only the MetaMorpheus 1.1.11 fixture exists (label-free); no isobaric or 1.2+ (BioPolymer ...) header table has been read through this verb yet.
