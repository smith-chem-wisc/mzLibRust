<!-- GENERATED by tests/spec_docs.rs from docs/specs/version.yaml (bridge 5db922d4cfe1). Do not edit: fix the spec in the bridge, sync it, re-render. -->

**Wire verb `version`**: Report the bridge's version, its wire protocol, its .NET runtime, the mzLib it was built from, and every verb it dispatches.

# Wraps

- [`AssemblyInformationalVersionAttribute`](https://github.com/smith-chem-wisc/mzLib/blob/23c2490e/mzLib/UsefulProteomicsDatabases/UsefulProteomicsDatabases.csproj) in `mzLib/UsefulProteomicsDatabases/UsefulProteomicsDatabases.csproj` at mzLib `23c2490e`

# Parameters

This verb takes no parameters.

# Returns

Top-level fields:

| Field | Type | Unit | Null? | Meaning |
|---|---|---|---|---|
| `bridge` | `string` | — | never | The bridge assembly's version. Not a compatibility number: use protocol. |
| `protocol` | `int` | — | never | The wire-format version; a binding is compatible with a bridge by this. |
| `runtime` | `string` | — | never | The bundled .NET runtime version. |
| `mzlib` | `string` | — | yes: the build recorded no mzLib commit | Which mzLib the bridge links, as 1.0.0+&lt;commit&gt;, read from the linked assembly. |
| `verbs` | `string[]` | — | never | Every verb this bridge dispatches, in dispatch order, GENERATED at build time from the DispatchAsync switch (BULK.md section 5). Absent from bridges before pyMzLib 0.2.0 (optional forever). A binding calling a verb its bridge does not list raises its usage error naming the release that has it, from that verb's since, instead of spawning a process. |

# Errors

| Kind | mzLibRust returns | When |
|---|---|---|
| `service_unavailable` | [`MzLibError::ServiceUnavailable`](crate::MzLibError::ServiceUnavailable) | never returned by this verb |

Any call can also fail on the way to mzLib or back, whatever the verb: [`MzLibError::BridgeNotFound`](crate::MzLibError::BridgeNotFound), [`MzLibError::Timeout`](crate::MzLibError::Timeout), [`MzLibError::Protocol`](crate::MzLibError::Protocol) or [`MzLibError::Io`](crate::MzLibError::Io).

# Caveats

- verbs lists what the bridge ROUTES, not which options each verb takes: a bulk option (--paths-stdin) on an older bridge that lists the verb still fails with that bridge's usage error.
- Bridges built before verbs existed omit the key; a binding must treat its absence as 'lists nothing', which correctly refuses every verb added since.
