<!-- GENERATED by tests/spec_docs.rs from docs/specs/readers.read-spectra.yaml (bridge 5db922d4cfe1). Do not edit: fix the spec in the bridge, sync it, re-render. -->

# Recorded examples

The examples on this page run in CI against these recordings from the live bridge, replayed by a stand-in bridge that answers only a call the recording fits:

- [`readers_spectra_mzml.json`](https://github.com/smith-chem-wisc/mzLibRust/blob/main/tests/fixtures/readers_spectra_mzml.json)
- [`readers_many_spectra.json`](https://github.com/smith-chem-wisc/mzLibRust/blob/main/tests/fixtures/readers_many_spectra.json)

# Same verb in other bindings

| Python (pyMzLib) | Rust (mzLibRust) | R (mzLibR) |
|---|---|---|
| `pymzlib.readers.read_spectra`; many inputs: `pymzlib.readers.read_spectra_many` | `mzlib::readers::read_spectra_with` with `SpectraOptions`; many inputs: `mzlib::readers::read_spectra_many` | `readers_read_spectra`; many inputs: `readers_read_spectra_many` |

# References

None: no publication is attached to this verb.

# Since

| Wire protocol | pyMzLib | mzLibRust | mzLibR |
|---|---|---|---|
| 1 | 0.1.0 | 0.1.0 | 0.1.0 |

# Not yet verified

The spec lists these as open; they are reported, never hidden:

- injection\_time unit 'ms' is from the mzML CV term (MS:1000927); not yet confirmed that every mzLib vendor reader (Thermo, Bruker, timsTOF, MGF) emits ms.
- source is read from MsDataFile.SourceFile after LoadAllStaticData; Bruker .d and timsTOF .d have not been checked for which of its fields they fill.
- The \_many spellings (py, rust, r) follow the 2026-09-23 cross-binding decision; Rust and R names here are intended, not yet ported, and since.pymzlib stays null until release.
