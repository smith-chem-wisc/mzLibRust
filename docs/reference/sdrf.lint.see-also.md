<!-- GENERATED by tests/spec_docs.rs from docs/specs/sdrf.lint.yaml (bridge 5db922d4cfe1). Do not edit: fix the spec in the bridge, sync it, re-render. -->

# Recorded examples

The examples on this page run in CI against these recordings from the live bridge, replayed by a stand-in bridge that answers only a call the recording fits:

- [`sdrf_lint_cohort.json`](https://github.com/smith-chem-wisc/mzLibRust/blob/main/tests/fixtures/sdrf_lint_cohort.json)

# Same verb in other bindings

| Python (pyMzLib) | Rust (mzLibRust) | R (mzLibR) |
|---|---|---|
| `pymzlib.sdrf.lint` | `mzlib::sdrf::lint_labelled` | `sdrf_lint` |

# References

- [doi:10.1038/s41467-021-26111-3](https://doi.org/10.1038/s41467-021-26111-3): SDRF-Proteomics and its controlled-vocabulary key=value cells (Dai et al., Nat Commun 12, 5854, 2021)

# Since

| Wire protocol | pyMzLib | mzLibRust | mzLibR |
|---|---|---|---|
| 1 | not yet shipped | not yet shipped | not yet shipped |

# Not yet verified

The spec lists these as open; they are reported, never hidden:

- Rust and R spellings are the intended ones, mirroring pool / pool\_labelled and sdrf\_pool; not yet ported.
- documents is the first list-valued cell in a columnar table on the wire. R will need a list column; confirm mzLibR's table projection accepts one before porting.
- since.pymzlib is null until released.
