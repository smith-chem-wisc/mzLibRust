<!-- GENERATED by tests/spec_docs.rs from docs/specs/sdrf.parse-age.yaml (bridge 5db922d4cfe1). Do not edit: fix the spec in the bridge, sync it, re-render. -->

# Recorded examples

The examples on this page run in CI against these recordings from the live bridge, replayed by a stand-in bridge that answers only a call the recording fits:

- [`sdrf_parse_age.json`](https://github.com/smith-chem-wisc/mzLibRust/blob/main/tests/fixtures/sdrf_parse_age.json)

# Same verb in other bindings

| Python (pyMzLib) | Rust (mzLibRust) | R (mzLibR) |
|---|---|---|
| `pymzlib.sdrf.parse_ages` | `mzlib::sdrf::parse_ages` | `sdrf_parse_ages` |

# References

- [doi:10.1038/s41467-021-26111-3](https://doi.org/10.1038/s41467-021-26111-3): SDRF-Proteomics age grammar nYnMnD (Dai et al., Nat Commun 12, 5854, 2021)

# Since

| Wire protocol | pyMzLib | mzLibRust | mzLibR |
|---|---|---|---|
| 1 | not yet shipped | next release (the spec has not recorded it yet) | not yet shipped |

# Not yet verified

The spec lists these as open; they are reported, never hidden:

- The refusal reason is a bridge-side classification of TryParse's false; see sdrf.samples.yaml. An upstream TryParse(cell, out age, out reason) would retire it.
- Binding name is parse\_ages (plural) for the verb parse-age (one cell per line). Kept because the function takes a list; revisit if a binding wants the wire name.
- Rust and R spellings are intended, not yet ported. R: NA\_character\_ in the input should map to an empty line, as Python maps None.
- since.pymzlib is null until released.
