<!-- GENERATED by tests/spec_docs.rs from docs/specs/version.yaml (bridge 5db922d4cfe1). Do not edit: fix the spec in the bridge, sync it, re-render. -->

# Recorded examples

The examples on this page run in CI against these recordings from the live bridge, replayed by a stand-in bridge that answers only a call the recording fits:

- [`bridge_version.json`](https://github.com/smith-chem-wisc/mzLibRust/blob/main/tests/fixtures/bridge_version.json)

# Same verb in other bindings

| Python (pyMzLib) | Rust (mzLibRust) | R (mzLibR) |
|---|---|---|
| `pymzlib.bridge_version` | `mzlib::bridge_version` | `bridge_version` |

# References

None: no publication is attached to this verb.

# Since

| Wire protocol | pyMzLib | mzLibRust | mzLibR |
|---|---|---|---|
| 1 | 0.1.0 | 0.1.0 | 0.1.0 |

# Not yet verified

The spec lists these as open; they are reported, never hidden:

- The Unknown-command message and verbs share one generated list (Program.Verbs, from the DeriveVerbList MSBuild task); check\_verbs.py still parses the switch itself. Both read the same pattern, so they cannot disagree unless the switch stops being one arm per line.
- pyMzLib returns version as a plain dict, typed as the TypedDict pymzlib.BridgeVersion so its keys are documented and linted; Rust and R spellings are the intended ones.
