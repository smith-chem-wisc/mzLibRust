# Byte-exact fixtures: their sha256 is part of what the proteins tests pin (search_database_sha256),
# so a CRLF checkout on Windows must not change them. The same rule as pyMzLib.
tests/fixtures/proteins/** -text
